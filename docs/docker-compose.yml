services:
  jekyll:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - .:/srv/jekyll
    # GitHub Pages needs /seeo-aws-orchestrator; local preview should open at /
    command: jekyll serve --host 0.0.0.0 --baseurl "" --watch --force_polling --incremental
