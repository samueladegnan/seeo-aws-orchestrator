# Backend base URL. Defaults to the local Rails server in development.
VITE_API_BASE=http://localhost:3000

# Public demo key for mock mode only. Never use a VITE_* value for real cloud access.
VITE_SEEO_API_KEY=local-development-only
